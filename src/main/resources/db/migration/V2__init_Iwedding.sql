CREATE TABLE iwedding_enterprise
(
    id              BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    enterprise_code VARCHAR(255),
    enterprise_name VARCHAR(255),
    thumbnail       VARCHAR(255),
    best            VARCHAR(255)
);
